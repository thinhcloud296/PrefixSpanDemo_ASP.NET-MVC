<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <title>@ViewBag.Title</title>
    <style>
        /* CSS từ trên được chèn vào đây */
        /* 1. Global & Typography */
        body {
            font-family: 'Roboto', Arial, sans-serif;
            background-color: #f0f7f4;
            color: #4a6741;
            line-height: 1.6;
            margin: 0;
            padding: 0;
        }

        /* 2. Header */
        .header {
            background-color: #ffffff;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.07);
            padding: 15px 30px;
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .content {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }

        .box-menu {
            flex: 1;
        }

        .all-list-menu {
            list-style: none;
            display: flex;
            padding: 0;
            margin: 0;
        }

        .all-list-menu li {
            margin-right: 20px;
        }

        .all-list-menu li.padding-list-menu {
            padding-left: 20px;
        }

        .all-list-menu a {
            color: #4a6741;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: color 0.2s ease;
        }

        .all-list-menu a:hover {
            color: #5cb85c;
        }

        .header img {
            max-width: 150px;
            height: auto;
        }

        .box-icon {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .box-heart, .box-user {
            font-size: 1.2rem;
        }

        .box-heart a, .box-user a {
            color: #4a6741;
            transition: color 0.2s ease;
        }

        .box-heart a:hover, .box-user a:hover {
            color: #5cb85c;
        }

        /* 3. Nội dung chính */
        .p-2 {
            padding: 20px !important;
            background-color: #f0f7f4;
            color: #4a6741;
        }

        /* 4. Footer chính */
        .footer {
            background-color: #4a6741;
            color: #ffffff;
            padding: 40px 30px;
            text-align: center;
            font-family: 'Roboto', Arial, sans-serif;
            font-size: 1rem;
            box-shadow: 0 -4px 15px rgba(0, 0, 0, 0.1);
        }

        .footer a {
            color: #5cb85c;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .footer a:hover {
            color: #ffffff;
        }

        /* 5. Box đầu tiên trong footer */
        .box-first-footer {
            background-color: #ffffff;
            border-radius: 12px;
            padding: 25px;
            margin: 0 auto 30px;
            max-width: 900px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.07);
            color: #4a6741;
            font-family: 'Roboto', Arial, sans-serif;
            text-align: center;
        }

        .box-first-footer h4 {
            font-size: 1.4rem;
            color: #2c3e50;
            margin-bottom: 15px;
        }

        .box-first-footer p {
            font-size: 1rem;
            margin: 0;
        }

        /* 6. Responsive */
        @@media (max-width: 1200px) {
            .content {
                padding: 0 15px;
            }
        }

        @@media (max-width: 992px) {
            .box-menu {
                display: none !important;
            }

            .header img {
                max-width: 120px;
            }
        }

        @@media (max-width: 767px) {
            .header {
                padding: 10px 15px;
            }

            .footer {
                padding: 30px 15px;
                font-size: 0.9rem;
            }

            .box-first-footer {
                padding: 20px;
                margin-bottom: 20px;
            }
        }

        @@media (max-width: 480px) {
            .header img {
                max-width: 100px;
            }

            .box-icon {
                gap: 10px;
            }

            .footer {
                padding: 20px 10px;
            }

            .box-first-footer {
                padding: 15px;
            }
        }
    </style>
    @RenderSection("head", required: false)
</head>

<body>
    <div class="header">
        <div class="content justify-content-center">
            <nav class="box-menu d-xl-block d-none w-50">
                <ul style="margin:0;" class="all-list-menu">
                    <li>
                        <a href="@Url.Action("Index","Home")" class="hover-a fs-6" style="font-weight: bold; font-size: 18px;">TRANG CHỦ</a>
                    </li>
                    <li class="padding-list-menu">
                        <a href="/xe/index" class="hover-a fs-6" style="font-weight: bold">SẢN PHẨM</a>
                    </li>
                    <li class="padding-list-menu">
                        <a href="/lienhe/index" class="hover-a fs-6" style="font-weight: bold">THÔNG TIN NHÓM</a>
                    </li>
                </ul>
            </nav>
            <a style="width:auto;height:auto;" class="d-flex justify-content-center align-items-center" href="@Url.Action("Index","Home")">
                
            </a>
            <div class="box-icon">
                <div class="box-heart" style="">
                    <a href="@Url.Action("Index","Favourite")">
                        <i class="fa-regular fa-heart heart"></i>
                    </a>
                </div>
                <div class="box-user" style="">
                    <a href="@Url.Action("Index","NhanVien")">
                        <i class="fa-regular fa-user user"></i>
                    </a>
                </div>
            </div>
        </div>
        @RenderSection("Banner", required: false)
    </div>

    <div class="p-2">@RenderBody()</div>
    <div id="footer" class="footer">
        <h4>Liên Hệ Với Chúng Tôi</h4>
        <p>Email: contact@raucu.vn | Điện thoại: (123) 456-7890</p>
        <p><a href="/about">Về chúng tôi</a> | <a href="/contact">Liên hệ</a> | <a href="/terms">Điều khoản</a></p>
    </div>
    <div class="box-first-footer">
        <h4>Cửa Hàng Rau Củ Thông Minh</h4>
        <p>Mang đến những sản phẩm tươi ngon nhất từ nông trại đến bàn ăn của bạn.</p>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="@Url.Content("~/Scripts/script.js")"></script>
    @RenderSection("scripts", required: false)
</body>
</html>